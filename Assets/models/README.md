# Optional ONNX model

Place a local `minilm.onnx` file in this folder if you want to experiment with ONNX Runtime later.

The application does **not** download models. If the file is missing, search continues with SQLite FTS5 and hashed TF-IDF.
