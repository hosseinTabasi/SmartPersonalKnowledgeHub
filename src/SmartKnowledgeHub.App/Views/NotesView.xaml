<UserControl x:Class="SmartKnowledgeHub.App.Views.NotesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <Grid DockPanel.Dock="Top" Margin="0,0,0,12">
            <TextBlock Text="Notes" Style="{StaticResource TitleText}"/>
            <TextBlock Text="{Binding StatusText}" Style="{StaticResource CaptionText}" HorizontalAlignment="Right" VerticalAlignment="Bottom"/>
        </Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0">
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Text="Notebooks" Style="{StaticResource CaptionText}" Margin="0,0,0,6"/>
                    <ComboBox ItemsSource="{Binding Notebooks}"
                              SelectedItem="{Binding SelectedNotebook}"
                              DisplayMemberPath="Name"
                              Margin="0,0,0,8"/>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding NewNotebookName, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="0,0,8,0"/>
                        <Button Grid.Column="1" Content="Add" Command="{Binding AddNotebookCommand}" Style="{StaticResource FluentButton}" Margin="0"/>
                    </Grid>
                    <TextBlock Text="Filter notes" Style="{StaticResource CaptionText}"/>
                    <TextBox Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,8"/>
                    <CheckBox Content="Show archived" IsChecked="{Binding ShowArchived}" Margin="0,0,0,8"/>
                    <Button Content="New note" Command="{Binding NewNoteCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,0,10" HorizontalAlignment="Stretch"/>
                </StackPanel>
                <Border Style="{StaticResource Card}" Padding="8">
                    <ListBox ItemsSource="{Binding Notes}" SelectedItem="{Binding SelectedNote}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="6">
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Style="{StaticResource CaptionText}" TextTrimming="CharacterEllipsis">
                                        <Run Text="{Binding NotebookName, Mode=OneWay}"/>
                                        <Run Text=" · "/>
                                        <Run Text="{Binding UpdatedUtc, Converter={StaticResource UtcToLocal}, ConverterParameter=g, Mode=OneWay}"/>
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>
            </DockPanel>

            <Border Grid.Column="2" Style="{StaticResource Card}"
                    Visibility="{Binding HasSelection, Converter={StaticResource BoolToVis}}">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Editor" Style="{StaticResource SectionTitle}"/>
                    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,12,0,0">
                        <Button Content="Save" Command="{Binding SaveNoteCommand}" Style="{StaticResource PrimaryButton}"/>
                        <Button Content="Pin / unpin" Command="{Binding TogglePinCommand}" Style="{StaticResource FluentButton}"/>
                        <Button Content="Delete" Command="{Binding DeleteNoteCommand}" Style="{StaticResource DangerButton}"/>
                    </StackPanel>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBox Grid.Row="0" Text="{Binding EditorTitle, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
                        <Grid Grid.Row="1" Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <ComboBox Grid.Column="0" ItemsSource="{Binding Notebooks}" SelectedItem="{Binding EditorNotebook}" DisplayMemberPath="Name"/>
                            <TextBox Grid.Column="2" Text="{Binding EditorTags, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>
                        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
                            <CheckBox Content="Pinned" IsChecked="{Binding EditorPinned}"/>
                            <CheckBox Content="Archived" IsChecked="{Binding EditorArchived}"/>
                        </StackPanel>
                        <TextBox Grid.Row="3" Text="{Binding EditorBody, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                                 FontFamily="Cascadia Mono, Consolas, Segoe UI"/>
                    </Grid>
                </DockPanel>
            </Border>
            <Border Grid.Column="2" Style="{StaticResource Card}"
                    Visibility="{Binding HasSelection, Converter={StaticResource InverseBoolToVis}}">
                <TextBlock Text="Select a note from the list, or create a new one."
                           Style="{StaticResource CaptionText}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
