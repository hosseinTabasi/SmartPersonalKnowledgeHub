<UserControl x:Class="SmartKnowledgeHub.App.Views.TasksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <Grid DockPanel.Dock="Top" Margin="0,0,0,12">
            <TextBlock Text="Tasks" Style="{StaticResource TitleText}"/>
            <TextBlock Text="{Binding StatusText}" Style="{StaticResource CaptionText}" HorizontalAlignment="Right" VerticalAlignment="Bottom"/>
        </Grid>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="All" Command="{Binding FilterAllCommand}" Style="{StaticResource FluentButton}"/>
            <Button Content="Todo" Command="{Binding FilterStatusCommand}" CommandParameter="Todo" Style="{StaticResource FluentButton}"/>
            <Button Content="Doing" Command="{Binding FilterStatusCommand}" CommandParameter="Doing" Style="{StaticResource FluentButton}"/>
            <Button Content="Done" Command="{Binding FilterStatusCommand}" CommandParameter="Done" Style="{StaticResource FluentButton}"/>
            <Button Content="New task" Command="{Binding NewTaskCommand}" Style="{StaticResource PrimaryButton}"/>
        </StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.1*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Style="{StaticResource Card}" Padding="8">
                <ListBox ItemsSource="{Binding Tasks}" SelectedItem="{Binding SelectedTask}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                    <TextBlock Style="{StaticResource CaptionText}">
                                        <Run Text="{Binding Status, Mode=OneWay}"/>
                                        <Run Text=" · "/>
                                        <Run Text="{Binding Priority, Converter={StaticResource PriorityText}, Mode=OneWay}"/>
                                        <Run Text=" · due "/>
                                        <Run Text="{Binding DueUtc, Converter={StaticResource UtcToLocal}, ConverterParameter=d, Mode=OneWay}"/>
                                    </TextBlock>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
            <Border Grid.Column="2" Style="{StaticResource Card}"
                    Visibility="{Binding HasSelection, Converter={StaticResource BoolToVis}}">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Edit task" Style="{StaticResource SectionTitle}"/>
                    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,12,0,0">
                        <Button Content="Save" Command="{Binding SaveTaskCommand}" Style="{StaticResource PrimaryButton}"/>
                        <Button Content="Mark done" Command="{Binding MarkDoneCommand}" Style="{StaticResource FluentButton}"/>
                        <Button Content="Delete" Command="{Binding DeleteTaskCommand}" Style="{StaticResource DangerButton}"/>
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Title" Style="{StaticResource CaptionText}"/>
                        <TextBox Text="{Binding EditorTitle, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
                        <TextBlock Text="Notes" Style="{StaticResource CaptionText}"/>
                        <TextBox Text="{Binding EditorBody, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="True" Height="90" TextWrapping="Wrap" Margin="0,0,0,8"/>
                        <TextBlock Text="Due date" Style="{StaticResource CaptionText}"/>
                        <DatePicker SelectedDate="{Binding EditorDue}" Margin="0,0,0,8"/>
                        <TextBlock Text="Priority" Style="{StaticResource CaptionText}"/>
                        <ComboBox ItemsSource="{Binding PriorityOptions}"
                                  SelectedValue="{Binding EditorPriority}"
                                  SelectedValuePath="Value"
                                  DisplayMemberPath="Label"
                                  Margin="0,0,0,8"/>
                        <TextBlock Text="Status" Style="{StaticResource CaptionText}"/>
                        <ComboBox ItemsSource="{Binding StatusOptions}" SelectedItem="{Binding EditorStatus}" Margin="0,0,0,8"/>
                        <TextBlock Text="Linked note" Style="{StaticResource CaptionText}"/>
                        <ComboBox ItemsSource="{Binding NoteChoices}"
                                  SelectedItem="{Binding EditorLinkedNote}"
                                  DisplayMemberPath="Title"/>
                    </StackPanel>
                </DockPanel>
            </Border>
            <Border Grid.Column="2" Style="{StaticResource Card}"
                    Visibility="{Binding HasSelection, Converter={StaticResource InverseBoolToVis}}">
                <TextBlock Text="Select a task to edit, or create a new one."
                           Style="{StaticResource CaptionText}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
