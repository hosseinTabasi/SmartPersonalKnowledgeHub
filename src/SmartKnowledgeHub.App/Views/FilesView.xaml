<UserControl x:Class="SmartKnowledgeHub.App.Views.FilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <Grid DockPanel.Dock="Top" Margin="0,0,0,12">
            <TextBlock Text="Files" Style="{StaticResource TitleText}"/>
            <TextBlock Text="{Binding StatusText}" Style="{StaticResource CaptionText}" HorizontalAlignment="Right" VerticalAlignment="Bottom" TextWrapping="Wrap" MaxWidth="520"/>
        </Grid>
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,12">
            <TextBlock Text="Tags for the next registration (comma separated)" Style="{StaticResource CaptionText}"/>
            <Grid Margin="0,4,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding TagsCsv, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0"/>
                <CheckBox Grid.Column="1" Content="Copy into vault" IsChecked="{Binding CopyIntoVault}" VerticalAlignment="Center"/>
                <Button Grid.Column="2" Content="Register file" Command="{Binding RegisterFileCommand}" Style="{StaticResource PrimaryButton}"/>
            </Grid>
        </StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Style="{StaticResource Card}" Padding="8">
                <ListBox ItemsSource="{Binding Files}" SelectedItem="{Binding SelectedFile}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="8">
                                <TextBlock Text="{Binding FileName}" FontWeight="SemiBold"/>
                                <TextBlock Style="{StaticResource CaptionText}">
                                    <Run Text="{Binding Extension, Mode=OneWay}"/>
                                    <Run Text=" · "/>
                                    <Run Text="{Binding SizeBytes, Converter={StaticResource BytesToSize}, Mode=OneWay}"/>
                                </TextBlock>
                                <TextBlock Text="{Binding OriginalPath}" Style="{StaticResource CaptionText}" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
            <Border Grid.Column="2" Style="{StaticResource Card}"
                    Visibility="{Binding HasSelection, Converter={StaticResource BoolToVis}}">
                <StackPanel>
                    <TextBlock Text="Selected file" Style="{StaticResource SectionTitle}"/>
                    <TextBlock Text="{Binding SelectedFile.FileName}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <TextBlock Text="Original path" Style="{StaticResource CaptionText}"/>
                    <TextBox Text="{Binding SelectedFile.OriginalPath, Mode=OneWay}" IsReadOnly="True" Margin="0,0,0,8"/>
                    <TextBlock Text="Vault path" Style="{StaticResource CaptionText}"/>
                    <TextBox Text="{Binding SelectedFile.VaultPath, Mode=OneWay}" IsReadOnly="True" Margin="0,0,0,8"/>
                    <TextBlock Text="Tags" Style="{StaticResource CaptionText}"/>
                    <TextBox Text="{Binding TagsCsv, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Open" Command="{Binding OpenFileCommand}" Style="{StaticResource PrimaryButton}"/>
                        <Button Content="Save tags" Command="{Binding SaveTagsCommand}" Style="{StaticResource FluentButton}"/>
                        <Button Content="Remove" Command="{Binding DeleteFileCommand}" Style="{StaticResource DangerButton}"/>
                    </StackPanel>
                    <TextBlock Margin="0,16,0,0" TextWrapping="Wrap" Style="{StaticResource CaptionText}"
                               Text="Text extraction runs for .txt, .md and .csv only. Binary files are registered as metadata. PDF OCR is out of scope."/>
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Style="{StaticResource Card}"
                    Visibility="{Binding HasSelection, Converter={StaticResource InverseBoolToVis}}">
                <TextBlock Text="Register a file to keep a path (and optional vault copy) in SQLite."
                           Style="{StaticResource CaptionText}" TextWrapping="Wrap" VerticalAlignment="Center"/>
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
